{{#paper-toolbar}}
  {{#paper-toolbar-tools}}
    {{#paper-button iconButton=true}}
      {{paper-icon "menu"}}
    {{/paper-button}}
    <img src="/assets/images/bread-icon-37186.png" class="layout-margin" width="48px" height="48px"/>
    <h2>
      DoughFlow
    </h2>
    <span class="flex"></span>
    {{#paper-button iconButton=true}}
      {{paper-icon "favorite"}}
    {{/paper-button}}
    {{#paper-button iconButton=true}}
      {{paper-icon "more_vert"}}
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}

<div class="layout-row">
  <div class="flex-10"></div>
  <div class="flex layout-margin">
    {{outlet}}
  </div>
  <div class="flex-10"></div>
</div>

